{
  "name": "tender-scraper-summarizer",
  "version": "1.0.0",
  "scripts": {
    "start": "cd api && npm install && node index.js",
    "build": "cd frontend && npm install && npm run build",
    "install:all": "npm install && cd scraper && npm install && pip3 install redis python-dotenv google-generativeai && cd ../api && npm install && cd ../frontend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
